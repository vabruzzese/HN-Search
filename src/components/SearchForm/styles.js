import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles({
    root: {
      width: 600,
      margin: '0 auto',
    },
    searchBar: {
        paddingTop: 5,
        paddingBottom: 20,
    },
    text: {
        paddingTop: 20,
        textAlign: 'center',
    },
});
